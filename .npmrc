auto-install-peers=true
strict-peer-dependencies=false
# React Native / Metro ບໍ່ເຂົ້າກັນກັບ pnpm symlink store — ໃຊ້ flat node_modules
# (Expo monorepo guidance). ກະທົບ backend/web-admin ໜ້ອຍ (flat = npm default).
node-linker=hoisted
