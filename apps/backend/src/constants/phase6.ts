/**
 * Phase 6 (Operations & Inventory) — tunables.
 */

/** AppSetting key ສຳລັບອັດຕາໂບນັດ KPI (ສ່ວນຂອງລາຍຮັບເກີນເປົ້າ). */
export const BONUS_RATE_SETTING_KEY = 'payroll.bonusRate';

/** ຄ່າ default ອັດຕາໂບນັດ ຖ້າ AppSetting ບໍ່ໄດ້ຕັ້ງ. clamp 0–0.5. */
export const BONUS_RATE_DEFAULT = 0.05;
